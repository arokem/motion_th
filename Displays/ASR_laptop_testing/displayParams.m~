function params = displayParams

% Critical parameters
params.numPixels = [800 600];
params.dimensions = [47 30];
params.distance = 61; %cm
params.frameRate = 75;
params.cmapDepth = 8;
params.screenNumber = 0;       
params.thisRect = [0 0 params.numPixels];

% Descriptive parameters
params.computerName = 'ariel_macbook';
params.monitor = 'built-in LCD';
params.card = 'GeForce8600'; 
params.position = 'Wherever the laptop happens to be :)';
