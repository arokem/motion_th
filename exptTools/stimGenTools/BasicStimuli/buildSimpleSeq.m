function seq = buildSimpleSeq(nVideoFrame, vfPerImage, imageNumList)% function seq = buildSimpleSeq(nVideoFrame, vfPerImage, imageNumList)%% Create a sequence of length nVideoFrame, containing (in order) vfPerImage repeats% of each element of imageNumList.  For example:%%    buildSimpleSeq(11, 3, [1 4 5]) yields [1 1 1 4 4 4 5 5 5 1 1].%% nVideoFrame = number of video frames in the sequence, including initial frame used for%    colormap setting.% vfPerImage = number of video frame repeats for each image% imageNumList = image indices to be used, in the order they're to be usedseq = [imageNumList ; repmat(0, vfPerImage-1, size(imageNumList, 2))];seq = seq(:);nRepeats = ceil(nVideoFrame/length(seq));seq = repmat(seq, nRepeats, 1);seq = seq(1:nVideoFrame)';