function DACtable = makeDACtable(gamma,step)%  DACTable = makeDACtable(gamma,step)%  creates the interpolated DACTable from interval measurements.% creates a DACTAble with 256 entries (assumes (N-1)*step = 255)% gamma should be in the form of Nx3% step = no of interpolation steps between measurements.% First enteries in gamma are set to zero (for inverting the gamma table%  in the future).gamma(1,1:3) = zeros(1,3);y1=interp(gamma(:,1),step);y2=interp(gamma(:,2),step);y3=interp(gamma(:,3),step);DACtable = [y1(1:256) y2(1:256) y3(1:256)];DACtable = DACtable ./ (ones(256,1)*max(DACtable));DACtable = truncToRange(DACtable,0,1);