function intensity = incrementIntensity(intensity,stepSize,logFlag)%intensity = incrementIntensity(intensity,stepSize,logFlag)% incremets an intensity value in eith erlinear of logarithmic steps.% Used in staircase code.if ~exist('logFlag') 	logFlag = 0;endif logFlag	goodVals = intensity~=0;	intensity(goodVals) = 10.^(log10(intensity(goodVals))+stepSize);else	intensity = intensity+stepSize;end