function stimulus = createStimulusStruct(img,cmap,seq,gamma)%stimulus = createStimulusStruct(img,cmap,seq,[gamma])%%Creates an stimulus structure with the fields:%%stimulus.image      array of images%stimulus.cmap       array of cmaps%stimulus.seq        array of indices into images and cmaps% if the optional gamma argument is passed in, then it is added to% the stimulus struct.  (See :exptTools:displayTools:tenBit2eightBit)stimulus.image = uint8(img);stimulus.cmap = cmap;stimulus.seq = seq;if exist('gamma','var')	stimulus.gamma = gamma;end