function [x,y] = makeVisAngleMesh(display,sz)%[x,y] = makeVisAngleMesh(display,[sz])%%Makes a meshgrid in visual angle coordinates, with (0,0) %in the center of the window.%%sz is the size of the mesh, in pixels.%if sz is not given, x and y are the size of the entire screen%3/5/98 gmb  Wrote it.if exist('sz')  m = sz(1);  n = sz(2);else  m = display.rect(4);  n = display.rect(3);end[x,y] = meshgrid(linspace(-pix2angle(display,n/2),pix2angle(display,n/2),n),...                 linspace(-pix2angle(display,m/2),pix2angle(display,m/2),m));