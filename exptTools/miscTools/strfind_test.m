function index = strfind(s,ca)% index = strfind(s,ca)%% Returns the index of ca that contain all or part of the string s.  Like% strmatch, ca may be a cell array.  Unlike ca, not all of the entries MUST% be strings.  strfind will skip over those entries that are not.  strfind% returns a 0 if it cannot find any occurences of s.%% See also STRMATCHindex = 0;for ii=1:length(ca)	if ischar(ca{ii})		x = strmatch(s,ca{ii});		if x			index = ii;			return;		end	endend