function [lowFBVal,highFBVal]=getFreeColors(display)% [lowFBVal,highFBVal]=getFreeColors(display)% % This return low and high framebuffer values that are free.% Other colors are reserved (see display.reservedColor).% % NOTE: colormap entries are indexed starting from 1 rather than 0% so the corresponding cmap indices range from lowFBVal+1 to highFBVal+1lowFBVal= max([display.reservedColor.fbVal])+1;highFBVal= display.numColors-1;