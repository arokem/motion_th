function rect = getRect(display,stimulus,centerFlag)%rect = getRect(display,stimulus,centerFlag)%%Returns the rectangle for placing a stimulus in screen%coordinates.  If centerFlag == 1, the rect is centered%the screen.  If not it is placed in the upper-left corner.%2/28/98 gmb wrote it.if ~exist('centerFlag')	centerFlag = 0;endrect = [0,0,size(stimulus.image,2),size(stimulus.image,1)];if centerFlag == 1	dx = round((display.rect(3)-rect(3))/2);	dy = round((display.rect(4)-rect(4))/2);	rect = rect + [dx,dy,dx,dy];end	